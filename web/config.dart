//Configuration
part of client;

final PORT = int.parse(Platform.environment['PORT']);
final HOST= int.parse(Platform.environment['HOST']);

//URI mapping
//var _mapping = {
//  "/": postJSON //mapping to a method
//};
